# IDM-VTON pip dependencies (mirrors the `pip:` section of environment.yaml).
#
# IMPORTANT: install PyTorch FIRST, then this file.
#   pip install torch==2.0.1 torchvision==0.15.2 torchaudio==2.0.2 \
#       --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
# `basicsr` imports torch inside its setup.py, so it fails if torch is absent.

# --- pins that are NOT optional -------------------------------------------
# diffusers 0.25.0 does `from huggingface_hub import cached_download`, which was
# removed in huggingface_hub 0.26.0. Anything >=0.26 breaks on import.
huggingface_hub==0.25.2
# torch 2.0.1 is not built against the NumPy 2 ABI.
numpy==1.26.4
# src/*_hacked_*.py subclass diffusers/transformers internals; these two pins
# must match what the paper's code was written against.
diffusers==0.25.0
transformers==4.36.2
# --------------------------------------------------------------------------

accelerate==0.25.0
torchmetrics==1.2.1
tqdm==4.66.1
einops==0.7.0
bitsandbytes==0.39.0
scipy==1.11.1
opencv-python
gradio==4.24.0
fvcore
cloudpickle
omegaconf
pycocotools
basicsr
av
# CPU provider only - preprocess/humanparsing/run_parsing.py hardcodes
# providers=['CPUExecutionProvider'].
onnxruntime==1.16.2
